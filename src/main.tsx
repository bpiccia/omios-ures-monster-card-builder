import React from 'react'
import ReactDOM from 'react-dom/client'
import MonsterCardMaker from './MonsterCardMaker.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MonsterCardMaker />
  </React.StrictMode>,
)
